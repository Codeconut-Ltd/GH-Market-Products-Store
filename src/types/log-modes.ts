/**
 * Log modes from browser console API.
 * Side note: Depending on logging mechanism, they are intended to either work as function or text.
 */
type LogModes = "error" | "info" | "log" | "warn"

export default LogModes
